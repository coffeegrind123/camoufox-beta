diff --git a/dom/base/FontListManager.cpp b/dom/base/FontListManager.cpp
index f70b4ef..2677ca8 100644
--- a/dom/base/FontListManager.cpp
+++ b/dom/base/FontListManager.cpp
@@ -1,6 +1,7 @@
 /* Camoufox: Per-context font list manager */
 #include "FontListManager.h"
 
+#include "gfxPlatformFontList.h"
 #include "mozilla/Mutex.h"
 #include "mozilla/dom/BrowsingContext.h"
 #include "nsGlobalWindowInner.h"
@@ -41,8 +42,29 @@ void FontListManager::SetFontList(uint32_t aId, const nsAString& aFontList) {
     ToLowerCase(utf8);
     fontSet.Insert(utf8);
   }
-  MutexAutoLock lock(sFontListMutex);
-  sFontLists.InsertOrUpdate(aId, std::move(fontSet));
+  {
+    MutexAutoLock lock(sFontListMutex);
+    auto current = sFontLists.Lookup(aId);
+    bool same = current && current.Data().Count() == fontSet.Count();
+    for (const auto& name : fontSet) {
+      if (!same) {
+        break;
+      }
+      same = current.Data().Contains(name);
+    }
+    if (same) {
+      return;
+    }
+    sFontLists.InsertOrUpdate(aId, std::move(fontSet));
+  }
+
+  // Outside sFontListMutex: the font list takes its own lock and then asks
+  // the gate, which takes sFontListMutex, so the reverse order here would
+  // invert them. Only a changed list clears, since each page's init script
+  // installs the same list again.
+  if (gfxPlatformFontList* pfl = gfxPlatformFontList::PlatformFontList(false)) {
+    pfl->ClearFontNameCaches();
+  }
 }
 
 bool FontListManager::HasFontList(uint32_t aId) {
diff --git a/gfx/thebes/gfxFcPlatformFontList.cpp b/gfx/thebes/gfxFcPlatformFontList.cpp
index 910cf2f..da37740 100644
--- a/gfx/thebes/gfxFcPlatformFontList.cpp
+++ b/gfx/thebes/gfxFcPlatformFontList.cpp
@@ -11,6 +11,7 @@
 #include "nsPresContext.h"
 #include "mozilla/dom/ContentChild.h"
 #include "mozilla/dom/ContentParent.h"
+#include "mozilla/dom/FontListManager.h"
 #include "mozilla/Preferences.h"
 #include "mozilla/Sprintf.h"
 #include "mozilla/StaticPrefs_gfx.h"
@@ -2434,6 +2435,13 @@ bool gfxFcPlatformFontList::FindAndAddFamiliesLocked(
                  : FontVisibility::User;
   cacheKey.Append(':');
   cacheKey.AppendInt(int(vis));
+  // Camoufox (lang315/camoufox#83): the per-context font gate lives in the
+  // base FindAndAddFamiliesLocked, which this memo short-circuits. Keyed by
+  // name alone, the first context to resolve a family answered for every
+  // context sharing the process: measured, a context whose list refuses
+  // Georgia rendered it once another context in its process had.
+  cacheKey.Append(':');
+  cacheKey.AppendInt(mozilla::dom::FontListManager::GetCurrentContext());
   if (const auto& cached = mFcSubstituteCache.Lookup(cacheKey)) {
     if (cached->IsEmpty()) {
       return false;
@@ -2704,6 +2712,10 @@ gfxPlatformFontList::PrefFontList* gfxFcPlatformFontList::FindGenericFamilies(
     }
   }
 
+  // Camoufox: same short-circuit as mFcSubstituteCache, for generics.
+  cacheKey.Append(':');
+  cacheKey.AppendInt(mozilla::dom::FontListManager::GetCurrentContext());
+
   // try to get the family from the cache
   return mGenericMappings.WithEntryHandle(
       cacheKey, [&](auto&& entry) -> PrefFontList* {
diff --git a/gfx/thebes/gfxFcPlatformFontList.h b/gfx/thebes/gfxFcPlatformFontList.h
index e055cda..485b049 100644
--- a/gfx/thebes/gfxFcPlatformFontList.h
+++ b/gfx/thebes/gfxFcPlatformFontList.h
@@ -317,6 +317,15 @@ class gfxFcPlatformFontList final : public gfxPlatformFontList {
     mGenericMappings.Clear();
   }
 
+  // Camoufox: both memos are keyed by context (see FindAndAddFamiliesLocked),
+  // but a context's entries taken before it installed its list were taken
+  // with the gate open, so an install drops them.
+  void ClearFontNameCaches() override {
+    AutoLock lock(mLock);
+    mFcSubstituteCache.Clear();
+    ClearGenericMappingsLocked();
+  }
+
   // map lang group ==> lang string
   void GetSampleLangForGroup(nsAtom* aLanguage, nsACString& aLangStr);
 
diff --git a/gfx/thebes/gfxPlatformFontList.h b/gfx/thebes/gfxPlatformFontList.h
index 7e98560..41a6721 100644
--- a/gfx/thebes/gfxPlatformFontList.h
+++ b/gfx/thebes/gfxPlatformFontList.h
@@ -330,6 +330,10 @@ class gfxPlatformFontList : public gfxFontInfoLoader {
   }
   virtual void ClearLangGroupPrefFontsLocked() MOZ_REQUIRES(mLock);
 
+  // Camoufox: drop memoized family-name answers when a context installs a
+  // new font list. A no-op here; the fontconfig list overrides it.
+  virtual void ClearFontNameCaches() {}
+
   void GetFontFamilyList(nsTArray<RefPtr<gfxFontFamily>>& aFamilyArray);
 
   already_AddRefed<gfxFont> SystemFindFontForChar(
