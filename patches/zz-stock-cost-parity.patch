diff --git a/dom/base/Navigator.cpp b/dom/base/Navigator.cpp
index 56f0ea1..6f63563 100644
--- a/dom/base/Navigator.cpp
+++ b/dom/base/Navigator.cpp
@@ -995,38 +995,43 @@ uint32_t Navigator::MaxTouchPoints(CallerType aCallerType) {
   // touchscreen laptop, which reports a digitizer here while keeping a fine
   // primary pointer. See force-default-pointer.patch for the matching
   // `any-pointer: coarse` half.
-  if (auto value = MaskConfig::GetUint32("navigator.maxTouchPoints")) {
-    return value.value();
-  }
-  nsIDocShell* docshell = GetDocShell();
-  BrowsingContext* bc = docshell ? docshell->GetBrowsingContext() : nullptr;
-
-  // Responsive Design Mode overrides the maxTouchPoints property when
-  // touch simulation is enabled.
-  if (bc && bc->Top()->InRDMPane()) {
-    return bc->Top()->GetMaxTouchPointsOverride();
-  }
+  // Camoufox: stock's work first, then the spoof (timing parity; see
+  // nsScreen::PixelDepth).
+  const uint32_t stock = [&]() -> uint32_t {
+    nsIDocShell* docshell = GetDocShell();
+    BrowsingContext* bc = docshell ? docshell->GetBrowsingContext() : nullptr;
+
+    // Responsive Design Mode overrides the maxTouchPoints property when
+    // touch simulation is enabled.
+    if (bc && bc->Top()->InRDMPane()) {
+      return bc->Top()->GetMaxTouchPointsOverride();
+    }
 
-  // The maxTouchPoints is going to reveal the detail of users' hardware. So,
-  // we will spoof it into 0 if fingerprinting resistance is on.
-  if (aCallerType != CallerType::System &&
-      nsContentUtils::ShouldResistFingerprinting(GetDocShell(),
-                                                 RFPTarget::MaxTouchPoints)) {
-    return SPOOFED_MAX_TOUCH_POINTS;
-  }
+    // The maxTouchPoints is going to reveal the detail of users' hardware. So,
+    // we will spoof it into 0 if fingerprinting resistance is on.
+    if (aCallerType != CallerType::System &&
+        nsContentUtils::ShouldResistFingerprinting(GetDocShell(),
+                                                   RFPTarget::MaxTouchPoints)) {
+      return SPOOFED_MAX_TOUCH_POINTS;
+    }
 
-  nsCOMPtr<nsIWidget> widget =
-      widget::WidgetUtils::DOMWindowToWidget(mWindow->GetOuterWindow());
+    nsCOMPtr<nsIWidget> widget =
+        widget::WidgetUtils::DOMWindowToWidget(mWindow->GetOuterWindow());
 
-  NS_ENSURE_TRUE(widget, 0);
-  uint32_t maxTouchPoints = widget->GetMaxTouchPoints();
+    NS_ENSURE_TRUE(widget, 0);
+    uint32_t maxTouchPoints = widget->GetMaxTouchPoints();
 
-  if (aCallerType != CallerType::System &&
-      nsContentUtils::ShouldResistFingerprinting(
-          GetDocShell(), RFPTarget::MaxTouchPointsCollapse)) {
-    return nsRFPService::CollapseMaxTouchPoints(maxTouchPoints);
+    if (aCallerType != CallerType::System &&
+        nsContentUtils::ShouldResistFingerprinting(
+            GetDocShell(), RFPTarget::MaxTouchPointsCollapse)) {
+      return nsRFPService::CollapseMaxTouchPoints(maxTouchPoints);
+    }
+    return maxTouchPoints;
+  }();
+  if (auto value = MaskConfig::GetUint32("navigator.maxTouchPoints")) {
+    return value.value();
   }
-  return maxTouchPoints;
+  return stock;
 }
 
 //*****************************************************************************
diff --git a/dom/base/nsGlobalWindowInner.cpp b/dom/base/nsGlobalWindowInner.cpp
index 7a974db..9076c70 100644
--- a/dom/base/nsGlobalWindowInner.cpp
+++ b/dom/base/nsGlobalWindowInner.cpp
@@ -3748,18 +3748,38 @@ nsresult nsGlobalWindowInner::GetInnerHeight(double* aHeight) {
 
 int32_t nsGlobalWindowInner::GetOuterWidth(CallerType aCallerType,
                                            ErrorResult& aError) {
-  if (auto value = MaskConfig::GetInt32("window.outerWidth"))
-    return value.value();
-  FORWARD_TO_OUTER_OR_THROW(GetOuterWidthOuter, (aCallerType, aError), aError,
+  // Camoufox: stock's work first, then the spoof (timing parity; see
+  // nsScreen::PixelDepth).
+  // Stock throws on an inactive window; so does this.
+  const int32_t stock = [&]() -> int32_t {
+    FORWARD_TO_OUTER_OR_THROW(GetOuterWidthOuter, (aCallerType, aError), aError,
                             0);
+  }();
+  if (aError.Failed()) {
+    return stock;
+  }
+  if (auto value = MaskConfig::GetInt32("window.outerWidth")) {
+    return value.value();
+  }
+  return stock;
 }
 
 int32_t nsGlobalWindowInner::GetOuterHeight(CallerType aCallerType,
                                             ErrorResult& aError) {
-  if (auto value = MaskConfig::GetInt32("window.outerHeight"))
-    return value.value();
-  FORWARD_TO_OUTER_OR_THROW(GetOuterHeightOuter, (aCallerType, aError), aError,
+  // Camoufox: stock's work first, then the spoof (timing parity; see
+  // nsScreen::PixelDepth).
+  // Stock throws on an inactive window; so does this.
+  const int32_t stock = [&]() -> int32_t {
+    FORWARD_TO_OUTER_OR_THROW(GetOuterHeightOuter, (aCallerType, aError), aError,
                             0);
+  }();
+  if (aError.Failed()) {
+    return stock;
+  }
+  if (auto value = MaskConfig::GetInt32("window.outerHeight")) {
+    return value.value();
+  }
+  return stock;
 }
 
 double nsGlobalWindowInner::ScreenEdgeSlopX() const {
@@ -3772,14 +3792,36 @@ double nsGlobalWindowInner::ScreenEdgeSlopY() const {
 
 int32_t nsGlobalWindowInner::GetScreenX(CallerType aCallerType,
                                         ErrorResult& aError) {
-  if (auto value = MaskConfig::GetInt32("window.screenX")) return value.value();
-  FORWARD_TO_OUTER_OR_THROW(GetScreenXOuter, (aCallerType, aError), aError, 0);
+  // Camoufox: stock's work first, then the spoof (timing parity; see
+  // nsScreen::PixelDepth).
+  // Stock throws on an inactive window; so does this.
+  const int32_t stock = [&]() -> int32_t {
+    FORWARD_TO_OUTER_OR_THROW(GetScreenXOuter, (aCallerType, aError), aError, 0);
+  }();
+  if (aError.Failed()) {
+    return stock;
+  }
+  if (auto value = MaskConfig::GetInt32("window.screenX")) {
+    return value.value();
+  }
+  return stock;
 }
 
 int32_t nsGlobalWindowInner::GetScreenY(CallerType aCallerType,
                                         ErrorResult& aError) {
-  if (auto value = MaskConfig::GetInt32("window.screenY")) return value.value();
-  FORWARD_TO_OUTER_OR_THROW(GetScreenYOuter, (aCallerType, aError), aError, 0);
+  // Camoufox: stock's work first, then the spoof (timing parity; see
+  // nsScreen::PixelDepth).
+  // Stock throws on an inactive window; so does this.
+  const int32_t stock = [&]() -> int32_t {
+    FORWARD_TO_OUTER_OR_THROW(GetScreenYOuter, (aCallerType, aError), aError, 0);
+  }();
+  if (aError.Failed()) {
+    return stock;
+  }
+  if (auto value = MaskConfig::GetInt32("window.screenY")) {
+    return value.value();
+  }
+  return stock;
 }
 
 float nsGlobalWindowInner::GetMozInnerScreenX(CallerType aCallerType,
diff --git a/dom/base/nsScreen.cpp b/dom/base/nsScreen.cpp
index d27c51a..d34ceb4 100644
--- a/dom/base/nsScreen.cpp
+++ b/dom/base/nsScreen.cpp
@@ -61,6 +61,22 @@ NS_IMPL_CYCLE_COLLECTION_INHERITED(nsScreen, DOMEventTargetHelper,
                                    mScreenOrientation)
 
 int32_t nsScreen::PixelDepth() {
+  // Camoufox: compute stock's answer first, then substitute the spoofed one.
+  // Returning the spoof before doing stock's work made this getter several
+  // times faster than stock Firefox's (screen.width 18-24 ns vs 107 ns,
+  // screen.colorDepth 10-15 vs 93, outerWidth 13 vs 129, maxTouchPoints 18
+  // vs 81), a ratio a page can time against an untouched getter.
+  const int32_t stock = [&]() -> int32_t {
+    // Return 24 to prevent fingerprinting.
+    if (ShouldResistFingerprinting(RFPTarget::ScreenPixelDepth)) {
+      return 24;
+    }
+    nsDeviceContext* context = GetDeviceContext();
+    if (NS_WARN_IF(!context)) {
+      return 24;
+    }
+    return context->GetDepth();
+  }();
   // Camoufox: per-context colour depth wins over the global config, so
   // contexts in one process can differ.
   if (uint32_t depth = 0; ScreenDimensionManager::GetColorDepth(
@@ -71,15 +87,7 @@ int32_t nsScreen::PixelDepth() {
     return value.value();
   if (auto value = MaskConfig::GetUint32("screen.pixelDepth"))
     return value.value();
-  // Return 24 to prevent fingerprinting.
-  if (ShouldResistFingerprinting(RFPTarget::ScreenPixelDepth)) {
-    return 24;
-  }
-  nsDeviceContext* context = GetDeviceContext();
-  if (NS_WARN_IF(!context)) {
-    return 24;
-  }
-  return context->GetDepth();
+  return stock;
 }
 
 nsPIDOMWindowOuter* nsScreen::GetOuter() const {
@@ -94,6 +102,39 @@ nsDeviceContext* nsScreen::GetDeviceContext() const {
 }
 
 CSSIntRect nsScreen::GetRect() {
+  // Camoufox: stock's work first, then the spoof (timing parity; see
+  // nsScreen::PixelDepth).
+  const CSSIntRect stock = [&]() -> CSSIntRect {
+    // Return window inner rect to prevent fingerprinting.
+    if (ShouldResistFingerprinting(RFPTarget::ScreenRect)) {
+      return GetTopWindowInnerRectForRFP();
+    }
+
+    // Here we manipulate the value of aRect to represent the screen size,
+    // if there is an override set with WebDriver BiDi or in RDM.
+    if (nsPIDOMWindowInner* owner = GetOwnerWindow()) {
+      if (Document* doc = owner->GetExtantDoc()) {
+        Maybe<CSSIntSize> deviceSize =
+            nsGlobalWindowOuter::GetRDMDeviceSize(*doc);
+        if (deviceSize.isSome()) {
+          const CSSIntSize& size = deviceSize.value();
+          return {0, 0, size.width, size.height};
+        }
+      }
+
+      if (BrowsingContext* bc = owner->GetBrowsingContext()) {
+        if (auto size = bc->GetScreenAreaOverride()) {
+          return {{}, *size};
+        }
+      }
+    }
+
+    nsDeviceContext* context = GetDeviceContext();
+    if (NS_WARN_IF(!context)) {
+      return {};
+    }
+    return CSSIntRect::FromAppUnitsRounded(context->GetRect());
+  }();
   // Camoufox: per-context size wins over the global config.
   if (uint32_t w = 0, h = 0; ScreenDimensionManager::GetDimensions(
           CamouUserContextId(GetOwnerWindow()), w, h)) {
@@ -105,38 +146,64 @@ CSSIntRect nsScreen::GetRect() {
       width && height) {
     return {0, 0, width.value(), height.value()};
   }
-  // Return window inner rect to prevent fingerprinting.
-  if (ShouldResistFingerprinting(RFPTarget::ScreenRect)) {
-    return GetTopWindowInnerRectForRFP();
-  }
+  return stock;
+}
 
-  // Here we manipulate the value of aRect to represent the screen size,
-  // if there is an override set with WebDriver BiDi or in RDM.
-  if (nsPIDOMWindowInner* owner = GetOwnerWindow()) {
-    if (Document* doc = owner->GetExtantDoc()) {
-      Maybe<CSSIntSize> deviceSize =
-          nsGlobalWindowOuter::GetRDMDeviceSize(*doc);
-      if (deviceSize.isSome()) {
-        const CSSIntSize& size = deviceSize.value();
-        return {0, 0, size.width, size.height};
-      }
+CSSIntRect nsScreen::GetAvailRect() {
+  // Camoufox: stock's work first, then the spoof (timing parity; see
+  // nsScreen::PixelDepth).
+  const CSSIntRect stock = [&]() -> CSSIntRect {
+    // Return window inner rect to prevent fingerprinting.
+    if (ShouldResistFingerprinting(RFPTarget::ScreenAvailRect)) {
+      return GetTopWindowInnerRectForRFP();
     }
 
-    if (BrowsingContext* bc = owner->GetBrowsingContext()) {
-      if (auto size = bc->GetScreenAreaOverride()) {
-        return {{}, *size};
+    // Check for overrides set by WebDriver BiDi or RDM before applying
+    // fingerprinting protection. This allows developer tools to simulate
+    // specific device dimensions for testing purposes.
+    if (nsPIDOMWindowInner* owner = GetOwnerWindow()) {
+      if (Document* doc = owner->GetExtantDoc()) {
+        Maybe<CSSIntSize> deviceSize =
+            nsGlobalWindowOuter::GetRDMDeviceSize(*doc);
+        if (deviceSize.isSome()) {
+          const CSSIntSize& size = deviceSize.value();
+          return {0, 0, size.width, size.height};
+        }
+      }
+
+      if (BrowsingContext* bc = owner->GetBrowsingContext()) {
+        if (auto size = bc->GetScreenAreaOverride()) {
+          return {{}, *size};
+        }
       }
     }
-  }
 
-  nsDeviceContext* context = GetDeviceContext();
-  if (NS_WARN_IF(!context)) {
-    return {};
-  }
-  return CSSIntRect::FromAppUnitsRounded(context->GetRect());
-}
+    if (ShouldResistFingerprinting(RFPTarget::ScreenAvailToResolution)) {
+      nsDeviceContext* context = GetDeviceContext();
+      if (NS_WARN_IF(!context)) {
+        return {};
+      }
 
-CSSIntRect nsScreen::GetAvailRect() {
+      if (nsPIDOMWindowInner* owner = GetOwnerWindow()) {
+        if (Document* doc = owner->GetExtantDoc()) {
+          AutoTArray<nsString, 1> params;
+          params.AppendElement(
+              u"https://support.mozilla.org/kb/firefox-protection-against-fingerprinting"_ns);
+          doc->WarnOnceAbout(Document::eScreenFingerprintingProtection, false,
+                             params);
+        }
+      }
+
+      return nsRFPService::GetSpoofedScreenAvailSize(
+          context->GetRect(), context->GetFullZoom(), IsFullscreen());
+    }
+
+    nsDeviceContext* context = GetDeviceContext();
+    if (NS_WARN_IF(!context)) {
+      return {};
+    }
+    return CSSIntRect::FromAppUnitsRounded(context->GetClientRect());
+  }();
   // Camoufox: the per-context API sets one size, so the available rect is the
   // whole screen. That matches the generated fingerprints, whose availWidth /
   // availHeight equal width / height, and keeps availWidth <= width true.
@@ -152,56 +219,7 @@ CSSIntRect nsScreen::GetAvailRect() {
     auto values = rect.value();
     return {values[0], values[1], values[2], values[3]};
   }
-  // Return window inner rect to prevent fingerprinting.
-  if (ShouldResistFingerprinting(RFPTarget::ScreenAvailRect)) {
-    return GetTopWindowInnerRectForRFP();
-  }
-
-  // Check for overrides set by WebDriver BiDi or RDM before applying
-  // fingerprinting protection. This allows developer tools to simulate
-  // specific device dimensions for testing purposes.
-  if (nsPIDOMWindowInner* owner = GetOwnerWindow()) {
-    if (Document* doc = owner->GetExtantDoc()) {
-      Maybe<CSSIntSize> deviceSize =
-          nsGlobalWindowOuter::GetRDMDeviceSize(*doc);
-      if (deviceSize.isSome()) {
-        const CSSIntSize& size = deviceSize.value();
-        return {0, 0, size.width, size.height};
-      }
-    }
-
-    if (BrowsingContext* bc = owner->GetBrowsingContext()) {
-      if (auto size = bc->GetScreenAreaOverride()) {
-        return {{}, *size};
-      }
-    }
-  }
-
-  if (ShouldResistFingerprinting(RFPTarget::ScreenAvailToResolution)) {
-    nsDeviceContext* context = GetDeviceContext();
-    if (NS_WARN_IF(!context)) {
-      return {};
-    }
-
-    if (nsPIDOMWindowInner* owner = GetOwnerWindow()) {
-      if (Document* doc = owner->GetExtantDoc()) {
-        AutoTArray<nsString, 1> params;
-        params.AppendElement(
-            u"https://support.mozilla.org/kb/firefox-protection-against-fingerprinting"_ns);
-        doc->WarnOnceAbout(Document::eScreenFingerprintingProtection, false,
-                           params);
-      }
-    }
-
-    return nsRFPService::GetSpoofedScreenAvailSize(
-        context->GetRect(), context->GetFullZoom(), IsFullscreen());
-  }
-
-  nsDeviceContext* context = GetDeviceContext();
-  if (NS_WARN_IF(!context)) {
-    return {};
-  }
-  return CSSIntRect::FromAppUnitsRounded(context->GetClientRect());
+  return stock;
 }
 
 bool nsScreen::IsFullscreen() const {
